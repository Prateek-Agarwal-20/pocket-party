<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".PlayProjectActivity"
        android:background="#000000">

    <ImageButton
            android:id="@+id/btnProjectPlay"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toTopOf="@id/sbProjectSeek"
            android:src="@drawable/ic_play_arrow_vec"
            android:background="@drawable/playbutton_background"
            android:scaleType="centerCrop"
            android:layout_width="300dp" android:layout_height="300dp"/>

    <SeekBar
            android:id="@+id/sbProjectSeek"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintBottom_toTopOf="@id/btnProjectLeftSeek"
            android:progressBackgroundTint="@color/backround_grey"
            android:progressTint="@color/colorPrimaryDark"
            android:thumbTint="@color/colorPrimary"
            android:layout_width="match_parent" android:layout_height="50dp"/>

    <ImageButton
            android:id="@+id/btnProjectLeftSeek"
            android:background="@drawable/ic_fast_rewind"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="80dp" android:layout_height="80dp"
            android:layout_marginBottom="30dp"
            android:layout_marginStart="90dp"/>

    <ImageButton
            android:id="@+id/btnProjectRightSeek"
            android:background="@drawable/ic_fast_forward"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="80dp" android:layout_height="80dp"
            android:layout_marginBottom="30dp"
            android:layout_marginEnd="90dp"/>

</android.support.constraint.ConstraintLayout>